
<%= form_with model: article do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <% if  article.new_record? %>
  <%= f.submit "Create Article" %>
  <% else %>
  <%= f.submit "Update Article" %>
  <% end %>
<% end %>
